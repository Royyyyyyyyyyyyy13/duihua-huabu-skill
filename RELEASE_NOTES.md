# 发布说明

## 0.1.0

发布名称：对话画布SKILL

内部插件名：`codex-canvas`

主要能力：

- 阶段 checkpoint 节点画布。
- n8n 风格节点和连线交互。
- 节点详情面板。
- 底部输入框组装区。
- 连线新增、删除、重连和撤销。
- 还原画布并支持撤销。
- 已有对话首次启用时，可基于最近可读 Codex 记录生成启示/回溯节点。
- 数据层包含 `schemaVersion`、`origin`、`confidence` 和插件版本字段。

验证状态：

- Skill 校验通过。
- 插件 manifest 校验通过。
- 前端 JS 语法检查通过。
- 完整画布回归测试通过。
