# 发布说明

## 0.1.1

修复：已有一个“画布启用”live 节点时，仍然允许自动回溯生成历史节点。

背景：

- 旧逻辑只要看到画布已有节点，就停止初始回溯。
- 这会导致另一个已经开发了多个版本的对话，只留下一个“画布启用”节点，没有把之前上下文补成历史节点。

本版调整：

- 前端在只有一个启动节点时仍触发 bootstrap。
- 后端识别启动节点后，会把回溯节点插到启动节点之前。
- 后端可以从启动节点的“项目目录”或相关文件推断真实工作区，避免复用旧服务进程 cwd 时匹配错会话。
- 回归测试新增“starter anchor backfill”场景。

验证状态：

- Skill 校验通过。
- 插件 manifest 校验通过。
- 前端 JS 语法检查通过。
- 完整画布回归测试通过。

## 0.1.0

发布名称：对话画布SKILL

内部插件名：`codex-canvas`

主要能力：

- 阶段 checkpoint 节点画布。
- n8n 风格节点和连线交互。
- 节点详情面板。
- 底部输入框组装区。
- 连线新增、删除、重连和撤销。
- 还原画布并支持撤销。
- 已有对话首次启用时，可基于最近可读 Codex 记录生成启示/回溯节点。
- 数据层包含 `schemaVersion`、`origin`、`confidence` 和插件版本字段。

验证状态：

- Skill 校验通过。
- 插件 manifest 校验通过。
- 前端 JS 语法检查通过。
- 完整画布回归测试通过。
