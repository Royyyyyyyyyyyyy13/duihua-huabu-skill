# 发布说明

## 0.1.3

修复：对话画布静默启用规则和 Skill 自身“记录后提示”规则存在冲突。

背景：

- 全局 `AGENTS.md` 已要求新对话默认静默记录画布 checkpoint。
- 旧 Skill 规则仍要求“最终回复里简短说明已记录 checkpoint”，这会导致未来 Codex 有时打破静默体验。

本版调整：

- Skill 支持 silent mode：当用户或全局指令要求静默记录时，成功写入 checkpoint 不再在最终回复里提示。
- 只有记录失败、出现阻塞，或用户主动询问画布状态时，才说明画布状态。

验证状态：

- 源 Skill 校验通过。
- 已安装缓存 Skill 校验通过。
- 插件 manifest 校验通过。
- 完整画布回归测试通过。

## 0.1.2

修复：历史回溯来源判断不可靠时，可能漏生成初始节点或误用别的对话记录。

背景：

- 画布服务是全局本地服务，不能把服务进程自己的 `cwd` 当成当前 Codex 对话目录。
- 前端旧触发条件只认“刚好 1 个画布启用节点”，如果启用后已经追加少量 live 节点，就不会再触发回溯。
- 泛关键词如 `codex`、`canvas`、`checkpoint`、`skill` 不能当作可靠会话匹配依据。
- `raw_memories.md` 只能作为有项目线索或 session 关键词命中的辅助来源，不能无条件兜底。

本版调整：

- `/bootstrap-anchor` 和 `/recover-raw-text` 不再默认使用服务端启动目录作为工作区线索。
- 前端允许“画布启用锚点 + 少量早期 live 节点”继续触发历史回溯，只要还没有 reconstructed 节点。
- 后端允许把回溯节点插到已有 live 节点之前，并保持主线连线完整。
- 回溯和原文回填都加入更严格的来源匹配：项目目录、相关文件、session 关键词至少命中其一；否则宁可不生成。
- Skill 文案明确：“画布启用”不等于历史已处理。

新增回归：

- 空画布无可靠来源时不生成假节点。
- 泛关键词不能匹配到无关 canvas 插件开发会话。
- 记忆文件不能无条件触发历史回溯或原文回填。
- 只有启动节点时能回填历史。
- 启动节点后已有早期 live 节点时仍能回填历史，并保持节点顺序和连线正确。

验证状态：

- Skill 校验通过。
- 插件 manifest 校验通过。
- 前端 JS 语法检查通过。
- 完整画布回归测试通过。
- 真实 `20260615-toucaicai-live` 旧备份复现通过：从 1 个启动节点自动生成 8 个回溯节点，并保持 9 节点 / 8 连线。

## 0.1.1

修复：已有一个“画布启用”live 节点时，仍然允许自动回溯生成历史节点。

背景：

- 旧逻辑只要看到画布已有节点，就停止初始回溯。
- 这会导致另一个已经开发了多个版本的对话，只留下一个“画布启用”节点，没有把之前上下文补成历史节点。

本版调整：

- 前端在只有一个启动节点时仍触发 bootstrap。
- 后端识别启动节点后，会把回溯节点插到启动节点之前。
- 后端可以从启动节点的“项目目录”或相关文件推断真实工作区，避免复用旧服务进程 cwd 时匹配错会话。
- 回归测试新增“starter anchor backfill”场景。

验证状态：

- Skill 校验通过。
- 插件 manifest 校验通过。
- 前端 JS 语法检查通过。
- 完整画布回归测试通过。

## 0.1.0

发布名称：对话画布SKILL

内部插件名：`codex-canvas`

主要能力：

- 阶段 checkpoint 节点画布。
- n8n 风格节点和连线交互。
- 节点详情面板。
- 底部输入框组装区。
- 连线新增、删除、重连和撤销。
- 还原画布并支持撤销。
- 已有对话首次启用时，可基于最近可读 Codex 记录生成启示/回溯节点。
- 数据层包含 `schemaVersion`、`origin`、`confidence` 和插件版本字段。

验证状态：

- Skill 校验通过。
- 插件 manifest 校验通过。
- 前端 JS 语法检查通过。
- 完整画布回归测试通过。
